CREATE TABLE EMPLOYEE (
    EMP_ID      CHAR(5)     NOT NULL,
    EMP_NAME    VARCHAR(50),
    DEPT_CODE   CHAR(3),
    SALARY      DECIMAL(9,2),
    CREATED_AT  DATE,
    UPDATED_AT  DATE,
    PRIMARY KEY (EMP_ID)
);
